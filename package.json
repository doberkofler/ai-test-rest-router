{
  "name": "ai-test-rest-router",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1"
  }
}
